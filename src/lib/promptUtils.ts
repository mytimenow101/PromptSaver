import {Folder,Prompt,SortOption} from "../types/prompt";
export const filterAndSortPrompts=(prompts:Prompt[],folders:Folder[],search:string,folderId:string,favOnly:boolean,sortBy:SortOption)=>{const q=search.toLowerCase();const m=prompts.filter(p=>{const fn=folders.find(f=>f.id===p.folderId)?.name.toLowerCase()||"";return (!q||[p.title,p.body,p.tags.join(" "),fn].join(" ").toLowerCase().includes(q))&&(!folderId||p.folderId===folderId)&&(!favOnly||p.favorite)});return m.sort((a,b)=>sortBy==="newest"?b.createdAt.localeCompare(a.createdAt):sortBy==="oldest"?a.createdAt.localeCompare(b.createdAt):sortBy==="mostUsed"?b.usageCount-a.usageCount:a.title.localeCompare(b.title));};
