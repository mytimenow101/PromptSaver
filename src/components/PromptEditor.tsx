import {Folder,Prompt} from "../types/prompt";
export function PromptEditor({folders,prompt,onSave,onCancel}:{folders:Folder[];prompt:Partial<Prompt>;onSave:(p:Partial<Prompt>)=>void;onCancel:()=>void;}){return <div className="p-3 space-y-2"><input className="w-full p-2 border rounded" placeholder="Title" value={prompt.title||""} onChange={e=>onSave({...prompt,title:e.target.value})}/><textarea className="w-full p-2 border rounded h-28" placeholder="Prompt body" value={prompt.body||""} onChange={e=>onSave({...prompt,body:e.target.value})}/><select className="w-full p-2 border rounded" value={prompt.folderId||folders[0]?.id} onChange={e=>onSave({...prompt,folderId:e.target.value})}>{folders.map(f=><option key={f.id} value={f.id}>{f.name}</option>)}</select><input className="w-full p-2 border rounded" placeholder="tags,comma" value={(prompt.tags||[]).join(",")} onChange={e=>onSave({...prompt,tags:e.target.value.split(",").map(t=>t.trim()).filter(Boolean)})}/><textarea className="w-full p-2 border rounded" placeholder="Notes" value={prompt.notes||""} onChange={e=>onSave({...prompt,notes:e.target.value})}/><label className="flex gap-2"><input type="checkbox" checked={prompt.favorite||false} onChange={e=>onSave({...prompt,favorite:e.target.checked})}/>Favorite</label><div className="flex justify-end gap-2"><button onClick={onCancel}>Cancel</button></div></div>}
